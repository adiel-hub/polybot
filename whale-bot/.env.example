# Telegram Bot Configuration
# Option 1: Use a dedicated whale alert bot token (recommended)
WHALE_BOT_TOKEN=your_bot_token_from_botfather

# Option 2: Or leave empty to use PolyBot's token from parent .env
# (requires TELEGRAM_BOT_TOKEN in PolyBot's .env)

# PolyBot username for "Trade Now" deep links (without @)
# This creates links like: https://t.me/YourPolyBot?start=m_abc123
POLYBOT_USERNAME=YourPolyBotUsername

# Whale Alert Threshold in USD (default: $10,000)
WHALE_THRESHOLD=10000

# Polling interval in seconds (default: 30)
POLL_INTERVAL=30

# Log Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
